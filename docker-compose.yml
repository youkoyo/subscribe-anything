services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DB_URL=/app/data/subscribe-anything.db
    restart: unless-stopped
